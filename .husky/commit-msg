# Check commit message format (optional - can be enhanced)
# For now, just ensure it's not empty
commit_msg=$(cat "$1")
if [ -z "$commit_msg" ]; then
  echo "❌ Commit message cannot be empty"
  exit 1
fi

